package com.amazon.logManager;

import org.apache.log4j.Logger;

public class MyLogger {

    public static Logger log= Logger.getLogger(MyLogger.class);   //Logger object is defined as static so that it can be directly in other classes.

    }



